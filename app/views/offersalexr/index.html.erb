<h1>Listing offers</h1>

<%= link_to 'Create a new offer', new_offer_path %>
<table>
  <tr>
    <th>Hey this is my Anuaire testing page ! </th>
    <th></th>
    <th></th>
  </tr>

  <td><%= link_to 'Annuaire', controller: 'annuaire' %></td>

  <% @offer.each do |offer| %>
    <tr>
      <td><%= offer.title %></td>
      <td><%= offer.text %></td>
      <td><%= link_to 'Show', offer_path(offeralexr) %></td>
      <td><%= link_to 'Edit', edit_offer_path(offeralexr) %></td>
      <td><%= link_to 'Destroy', offer_path(offeralexr),
             method: :delete,
             data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
</table>
